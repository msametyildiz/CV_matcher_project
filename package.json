{
  "name": "cv-matcher",
  "version": "1.0.0",
  "description": "CV Matcher Application with Backend and Frontend",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "leaflet": "^1.9.4",
    "multer": "^1.4.5-lts.2",
    "react-hot-toast": "^2.5.2",
    "react-leaflet": "^5.0.0"
  }
}
